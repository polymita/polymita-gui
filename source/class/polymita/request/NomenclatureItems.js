qx.Class.define("polymita.request.NomenclatureItems", {
    extend: polymita.request.AbstractResource,

    construct: function (sync) {
        this.base(arguments, 'NomenclatureItems', sync);
    }
});
